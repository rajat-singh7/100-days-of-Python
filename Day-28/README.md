# Function Caching in Python

**Caching in Python is a performance optimization technique where results of expensive function calls are stored and reused for identical inputs. This avoids recomputation and speeds up repeated calls.**